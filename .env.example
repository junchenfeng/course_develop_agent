# 仅供参考：Claude Code 原生模式不会自动读取项目 .env 文件。
# 请把这些变量配置到 ~/.zshrc 或项目级 .claude/settings.local.json 中。

OPENROUTER_API_KEY=
ANTHROPIC_BASE_URL=https://openrouter.ai/api
ANTHROPIC_AUTH_TOKEN=$OPENROUTER_API_KEY
ANTHROPIC_API_KEY=
ANTHROPIC_DEFAULT_OPUS_MODEL=anthropic/claude-opus-4.6
ANTHROPIC_DEFAULT_SONNET_MODEL=xiaomi/mimo-v2-pro
ANTHROPIC_DEFAULT_HAIKU_MODEL=xiaomi/mimo-v2-omni
CLAUDE_CODE_SUBAGENT_MODEL=xiaomi/mimo-v2-pro

# 可选：OpenRouter 统计/展示用
# OPENROUTER_HTTP_REFERER=https://your-site.example
# OPENROUTER_APP_TITLE=course_develop_agent

# ── Azure OpenAI / Azure AI Foundry（对话诊断用） ──
# 使用 Responses API；脚本会自动将 URL 规范化为 /openai/v1/ 后缀
AZURE_OPENAI_BASE_URL=https://your-resource.cognitiveservices.azure.com/openai/v1/
AZURE_OPENAI_API_KEY=your-api-key-here

# 诊断用模型部署名（GPT-5.4）
AZURE_OPENAI_DIAGNOSIS_DEPLOYMENT=gpt-5.4

# 汇总分析用模型部署名（GPT-5.4 Pro）。若留空或删除，汇总脚本回退到
# AZURE_OPENAI_DIAGNOSIS_DEPLOYMENT
AZURE_OPENAI_SUMMARY_DEPLOYMENT=gpt-5.4-pro

# 并发控制（诊断脚本同时运行的 API 请求数）
DIAGNOSIS_MAX_CONCURRENCY=5
